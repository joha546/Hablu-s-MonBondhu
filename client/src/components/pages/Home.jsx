import React from 'react';
import Navbar from '../ui/Navbar';
import Hero from '../ui/Hero';

const Home = () => {
    return (
        <div>
            <Navbar />
            <Hero />
        </div>
    );
};

export default Home;